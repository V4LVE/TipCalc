<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TipCalc.Views.MonkeyPage"
             xmlns:model="clr-namespace:TipCalc.Models"
             xmlns:local="clr-namespace:TipCalc.Views"
             xmlns:viewmodel="clr-namespace:TipCalc.ViewModels"
             Title="RestaurantListPage"
             x:DataType="viewmodel:MonkeyPageViewModel">

    <ContentPage.ToolbarItems>
        
    </ContentPage.ToolbarItems>

    <CollectionView ItemsSource="{Binding Monkeys}" SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:Monkey">
                <Grid Padding="10">
                    <Border Shadow="True" HeightRequest="125">
                        <Grid Padding="0" ColumnDefinitions="125,*">
                            <Image
                                Aspect="AspectFill"
                                HeightRequest="125"
                                Source="{Binding Image}"
                                WidthRequest="125" />
                            <VerticalStackLayout Grid.Column="1" Padding="10">
                                <Label Text="{Binding Name}" />
                                <Label Text="{Binding Details}" />
                                <Label Text="{Binding Population}" />
                                <Label Text="{Binding Longitude}" />
                                <Label Text="{Binding Latitude}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>