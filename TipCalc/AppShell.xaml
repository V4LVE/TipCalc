<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TipCalc.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TipCalc"
    xmlns:views="clr-namespace:TipCalc.Views"
    xmlns:modals="clr-namespace:TipCalc.Views.Modals"
    Title="TipCalc"
    Shell.FlyoutBehavior="Flyout">
    
    
    <FlyoutItem Title="Home" Icon="home.png">
        <ShellContent Title="Home"
                      ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="About" Icon="info.png">
        <ShellContent Title="About"
                      ContentTemplate="{DataTemplate views:AboutPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Feedback" Icon="feedback.png">
        <ShellContent Title="Feedback"
                      ContentTemplate="{DataTemplate modals:FeedbackPage}" />
    </FlyoutItem>
</Shell>
